{# {% extends 'base.html.twig' %}

{% block title %}home.html.twig{% endblock %}

{% block body %}
                <div {{ react_component('Page_Contact') }}></div>
{% endblock %} #}

{% extends 'base.html.twig' %} 
{% block title %}Contact{% endblock %}

{% block body %}
    <div {{ react_component('Page_Contact') }}>
         {# <div class="container">
        <h1>Contactez-nous</h1>
        
        <form method="post" action="{{ path('app_contact') }}">
            {{ form_start(form) }}
            
            {{ form_row(form.nom) }}
            {{ form_row(form.prenom) }}
            {{ form_row(form.email) }}
            {{ form_row(form.message) }}
            
            <button type="submit">Envoyer le message</button>
            
            {{ form_end(form) }}
        </form>
    </div> #}
    </div>
   
{% endblock %}